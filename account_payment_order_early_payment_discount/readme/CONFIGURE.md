No configuration is required. The discount is applied automatically to every
eligible payment line (there is no opt-in setting in this version). Users can
still opt a line out (or back in) manually via the "Pay with Discount" toggle
on the payment line, as long as the payment order is editable.
